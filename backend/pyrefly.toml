# Pyrefly configuration
# Points to the project's virtual environment so all installed packages resolve correctly.

python_interpreter = "./venv/Scripts/python.exe"

[python_environment]
venv_path = "./venv"
